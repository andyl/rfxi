defmodule RfxiWeb.EchoSocketTest do
  use RfxiWeb.ChannelCase

  alias RfxiWeb.EchoSocket

  describe "connect" do
    # test "connect without parameters" do
    #   connect(EchoSocket, %{}) |> IO.inspect()
    #   assert true
    # end
  end

end
